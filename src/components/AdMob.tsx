import React from 'react';
import {UserProfile} from '../types';

export async function initAdMob(): Promise<void> {}
export function AdBanner({profile}: {profile: UserProfile | null}): null { return null; }
export function AppOpenAd({profile}: {profile: UserProfile | null}): null { return null; }
