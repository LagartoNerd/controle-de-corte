{
  "name": "controle-de-corte-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions - Controle de Corte",
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "mercadopago": "^2.12.0",
    "cors": "^2.8.6",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/cors": "^2.8.17",
    "@types/node": "^20.0.0"
  }
}
