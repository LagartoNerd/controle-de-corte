# Controle de Corte — Variáveis de Ambiente
# Copie este arquivo para .env e preencha os valores

# Firebase (já configurado no src/firebase.ts — não precisa mudar)
# FIREBASE_PROJECT_ID=ai-studio-applet-webapp-91ce3

# URL base do app (usada nas Cloud Functions para redirect)
# APP_URL=https://seu-dominio.com

# Ambiente
NODE_ENV=development
